apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mockapi.fullname" . }}-test-connection"
  labels:
    {{- include "mockapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mockapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
